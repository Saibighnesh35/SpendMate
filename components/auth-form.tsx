"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { useSpendMate } from "./demo-provider";

export function AuthForm({mode}:{mode:"login"|"signup"}) {const {login}=useSpendMate();const router=useRouter();const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const submit=(e:React.FormEvent)=>{e.preventDefault();if(mode==="signup"&&!name.trim()){setError("Enter your name.");return;}if(!/^\S+@\S+\.\S+$/.test(email)){setError("Enter a valid email address.");return;}if(password.length<6){setError("Use at least 6 characters for your password.");return;}login(name.trim()||email.split("@")[0],email.trim());router.push("/dashboard")};return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20,background:"radial-gradient(circle at 10% 10%,#e6f7ed,transparent 32%)"}}><form onSubmit={submit} className="card" style={{width:"min(100%,440px)",padding:"clamp(24px,5vw,38px)"}}><Link href="/" style={{fontWeight:800,fontSize:"1.2rem",letterSpacing:"-.04em"}}>spend<span style={{color:"var(--brand)"}}>mate</span></Link><p className="eyebrow" style={{marginTop:31}}>{mode==="signup"?"Get started":"Welcome back"}</p><h1 className="page-title" style={{fontSize:"2rem"}}>{mode==="signup"?"Start tracking today.":"Log in to SpendMate."}</h1><p className="muted" style={{lineHeight:1.5}}>{mode==="signup"?"A focused place for your money in, money out, and monthly clarity.":"Continue your spending story."}</p><div className="grid" style={{marginTop:22}}>{mode==="signup"&&<label><span className="label">Name</span><input className="field" value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" autoComplete="name"/></label>}<label><span className="label">Email</span><input className="field" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" type="email" autoComplete="email"/></label><label><span className="label">Password</span><input className="field" value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 6 characters" type="password" autoComplete={mode==="signup"?"new-password":"current-password"}/></label></div>{error&&<p style={{color:"var(--danger)",fontSize:".88rem"}}>{error}</p>}<button className="btn btn-primary" style={{width:"100%",marginTop:22}} type="submit">{mode==="signup"?"Create account":"Log in"}</button>{mode==="login"&&<Link href="/forgot-password" style={{display:"block",fontSize:".86rem",color:"var(--brand)",fontWeight:700,textAlign:"center",marginTop:15}}>Forgot password?</Link>}<p className="muted" style={{fontSize:".83rem",textAlign:"center",marginTop:22}}>{mode==="signup"?"Already have an account?":"New to SpendMate?"} <Link href={mode==="signup"?"/login":"/signup"} style={{color:"var(--brand)",fontWeight:700}}>{mode==="signup"?"Log in":"Create an account"}</Link></p><p style={{fontSize:".72rem",lineHeight:1.4,background:"#f7faf8",padding:10,borderRadius:9,color:"var(--muted)"}}>Local preview mode stores this sample account in your browser. Configure Supabase to use production authentication.</p></form></main>}
