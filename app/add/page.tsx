import Link from "next/link";
import { Camera, PenLine } from "lucide-react";
import { AppShell } from "@/components/app-shell";
export default function Page(){return <AppShell><div style={{maxWidth:760,margin:"25px auto"}}><p className="eyebrow">Fast capture</p><h1 className="page-title">How would you like to add this?</h1><p className="muted">Choose the fastest way for this transaction.</p><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(260px,1fr))",gap:16,marginTop:26}}><Link href="/add/screenshot" className="card" style={{padding:27,borderColor:"#a9dfc2"}}><Camera size={28} color="var(--brand)"/><h2>Upload screenshot</h2><p className="muted">We’ll read the details, then you check and confirm every field.</p><strong style={{color:"var(--brand)"}}>Choose screenshot →</strong></Link><Link href="/add/manual" className="card" style={{padding:27}}><PenLine size={28} color="var(--brand)"/><h2>Enter manually</h2><p className="muted">Record an expense or credit in just a few fields.</p><strong style={{color:"var(--brand)"}}>Enter manually →</strong></Link></div></div></AppShell>}
