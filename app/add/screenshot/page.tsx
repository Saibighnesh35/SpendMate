"use client";
import { useRef, useState } from "react";
import { useRouter } from "next/navigation";
import { Camera, ImageUp, ShieldCheck } from "lucide-react";
import { AppShell } from "@/components/app-shell";

export default function Page(){const router=useRouter();const ref=useRef<HTMLInputElement>(null);const [file,setFile]=useState<File>();const [busy,setBusy]=useState(false);const accept=(picked?:File)=>{if(!picked)return;if(!picked.type.startsWith("image/")||picked.size>10*1024*1024)return;setFile(picked)};const process=()=>{if(!file)return;setBusy(true);setTimeout(()=>router.push("/add/screenshot/review"),700)};return <AppShell><div style={{maxWidth:700,margin:"25px auto"}}><p className="eyebrow">Secure upload</p><h1 className="page-title">Upload a payment screenshot</h1><p className="muted">We only use the image to suggest transaction details. You always confirm before anything is saved.</p><button onClick={()=>ref.current?.click()} onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();accept(e.dataTransfer.files[0])}} className="card" style={{width:"100%",padding:42,marginTop:24,borderStyle:"dashed",borderColor:"#8bcaaa",background:file?"#f6fcf8":"#fff",textAlign:"center"}}><ImageUp size={32} color="var(--brand)"/><h2>{file?file.name:"Drop your image here"}</h2><p className="muted">or select it from your device · PNG, JPG, WEBP up to 10 MB</p></button><input ref={ref} hidden type="file" accept="image/png,image/jpeg,image/webp" onChange={e=>accept(e.target.files?.[0])}/>{file&&<div style={{display:"flex",gap:10,marginTop:16,alignItems:"center"}}><span style={{display:"flex",gap:7,alignItems:"center",fontSize:".86rem",color:"var(--muted)",flex:1}}><ShieldCheck size={17} color="var(--brand)"/> Private attachment — delete it later in Privacy.</span><button className="btn btn-primary" onClick={process}>{busy?"Reading your transaction…":"Continue"}</button></div>}</div></AppShell>}
